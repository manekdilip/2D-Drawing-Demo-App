name: countertop_drawer
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dxf: ^2.1.0
  path_provider: ^2.1.3
  path: ^1.9.0
  flutter_email_sender: ^6.0.3
  get: ^4.6.6
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true